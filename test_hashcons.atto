(* Test hash-consing behavior *)

open EGraph

let eg0 = EGraph.empty 0 in
let (id1, eg1) = EGraph.add (eg0, Expr.Const 42.0) in
let (id2, eg2) = EGraph.add (eg1, Expr.Const 42.0) in
let nextId1 = EGraph.nextId eg1 in
let nextId2 = EGraph.nextId eg2 in
(id1, id2, nextId1, nextId2)
